<p id="notice"><%= notice %></p>

<div class="container">

  <div class="row">
  <% @images.each do |image| %>
    <div class="col-md-4">
      <div class="thumbnail">
        <div><%= link_to (image_tag image.file_url(:thumb)), image %></td></div>
          <div class="caption">
          <div class="row">
          <div class="col-md-4">

            <%= link_to 'Edit', edit_image_path(image) %>||<%= link_to 'Destroy', image, method: :delete, data: { confirm: 'Are you sure?' } %></p>
          </div>
          <div class="coll-md-8 right">
            <%= image.title %></br>
            <span class="text-muted">uploaded by</span>
            <%= image.user.name %>
          </div>
          </div>
          </div>
        </a>
      </div>
    </div>
    <% end %>
  </div>

</div>




<br>

<%= link_to 'New Image', new_image_path %>
